# Copy to .env and fill values for development
# Example environment variables for Smart-Crop-Tools

# If you have a local MongoDB instance, set this to your connection string.
# If omitted, the app will use an in-memory datastore suitable for development/testing.
MONGODB_URI=

# Dev server port (optional). If not set, defaults to 8080 and Vite may pick another free port.
PORT=8080

# Optional message returned by /api/ping
PING_MESSAGE=ping

# Example: Netlify Neon DB (used only in Netlify serverless deployments)
# NETLIFY_DATABASE_URL=
